{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the LocalStream main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
