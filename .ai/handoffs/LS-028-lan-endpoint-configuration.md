# LS-028 Handoff — Explicit LAN endpoint configuration

Completed 2026-08-18. Configuration is persisted, disabled by default, and accepts one explicit private/link-local unicast address, non-privileged port, and optional validated DNS name. Loopback, wildcard, multicast, broadcast, public, and malformed values fail. Configuration alone never binds.

Next dependency: LS-029.
